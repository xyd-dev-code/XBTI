# XBTI 部署清单（Cloudflare Pages · Git 关联）

前提：已有 Cloudflare 账号、GitHub / GitLab 账号；本仓库根即 `site/`，输出目录填 `.`。
⚠ 三个最易漏项已标 ★。

## 一、上线前准备（本地，已完成大部分）
- [x] 1. `assets/js/config.js` 中 `SUBMIT_ENDPOINT: "/api/submit"`（同域相对路径，任何域名通用）★易漏
- [x] 2. `functions/api/submit.js`、`functions/api/admin.js` 已在 `site/functions/` 下
- [ ] 3. （可选）本地试跑：`python -m http.server 8080` → 打开 `http://localhost:8080`，走一遍答题（无后端时答案存 localStorage，不报错）

## 二、推送到 Git 仓库（本仓库已 init 并 commit）
- [ ] 4. 在 GitHub / GitLab 新建一个**空仓库**（不要勾勾选 README/.gitignore）
- [ ] 5. 添加远程并推送：
  ```bash
  git remote add origin <你的仓库地址>
  git push -u origin main        # 若默认分支是 master，用 git push -u origin master
  ```

## 三、Cloudflare Pages 关联（Git）
- [ ] 6. Cloudflare 面板 → Workers & Pages → 创建 → Pages → 连接 Git → 选中该仓库
- [ ] 7. 构建设置：**构建命令 留空**，**输出目录 填 `.`**（仓库根即 site，functions 也在根）★易漏（填 `site` 会导致函数不被识别）
  - ⚠️ **致命坑（已踩过）**：Git 关联部署时，仓库根目录**绝不能放 `wrangler.toml`**。Cloudflare 一旦检测到它，会把项目误判成 Wrangler/Workers 项目，自动把构建命令填成 `npx wrangler deploy`，导致部署跑偏/失败。本项目已删除该文件。`wrangler.toml` 仅用于「`wrangler pages deploy` 命令行部署」这一种方式，与 Git 关联二选一，不要共存。
- [ ] 8. 保存并首次部署；完成后得到 `xxx.pages.dev`（可在 Settings → 自定义域 绑你自己的域名）

## 四、数据库与令牌（仅首次）
- [ ] 9. 建库并建表（任选其一）：
  - CLI：`wrangler d1 create xbti` → 把 `database_id` 填进 `wrangler.toml`；再 `wrangler d1 execute xbti --remote --file=./schema.sql`
  - 面板：Cloudflare → D1 → 新建数据库 `xbti` → 打开 SQL 编辑器粘贴执行 `schema.sql` 内容
- [ ] 10. 绑定 D1：Cloudflare → Pages → 项目 → Settings → Functions → D1 数据库绑定 → 添加，名称填 **`XBTI_DB`**，选择数据库 `xbti` ★易漏（不设则函数读不到库）
- [ ] 11. 设令牌：
  - CLI：`wrangler pages secret put XBTI_ADMIN_TOKEN`，输入你自定义的密码
  - 面板：Pages → Settings → 环境变量 → 添加 `XBTI_ADMIN_TOKEN`（生产环境），值填你的密码

## 五、验证
- [ ] 12. 打开你的域名，答完题到诊断书，确认流程通
- [ ] 13. 访问 `域名/admin.html`，输入令牌 → 看到刚那条记录、可按人格筛选、导出 CSV
- [ ] 14. 报告页「复制判词 / 复制诊断文案 / 保存雷达图」自测通过

## 备注
- 若你希望整个 `XBTI/` 作为仓库（而非 `site/` 为根）：把 `functions/` 移到 `XBTI/functions/`、`wrangler.toml` 放 `XBTI/` 并设 `pages_build_output_dir = "site"`，输出目录填 `site`，其余步骤不变。
- 没有 wrangler 也能上线：第 9–11 步全部可在 Cloudflare 面板完成，无需本地 CLI。
- 本地只双击 `index.html`（无后端）时不需令牌，答案存浏览器 localStorage，仅供自测。
